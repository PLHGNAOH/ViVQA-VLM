# pin dependencies here

# prepare_vivqa.py (tải + chuẩn bị dataset ViVQA)
pandas
requests
tqdm

# prepare_vitextvqa.py (tải ViTextVQA chính thức từ HF, gated)
huggingface_hub

# đọc configs/*.yaml (run_baseline.py, notebook)
pyyaml
