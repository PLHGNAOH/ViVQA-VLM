# experiments/

Mỗi run một thư mục: `Wxx_<tên>/` (ví dụ `W05_baseline_qwen25vl/`).

Bắt buộc trong README run: split, seed, hardware, hyperparameters, EM, VQA-Acc, ANLS, train time, infer time.

Template: `docs/templates/Experiment_Log_Template.md`.
